<Window x:Class="ExplicitTaskbar.Tray"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:ExplicitTaskbar"
        xmlns:tb="http://www.hardcodet.net/taskbar"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="Tray" Height="450" Width="800"
        Visibility="Hidden">
    <Window.DataContext>
        <local:TrayViewModel/>
    </Window.DataContext>
        
    <tb:TaskbarIcon x:Name="MyNotifyIcon" 
                    IconSource="/Assets/icon.ico"
                    ToolTipText="Explicit Taskbar">
           <tb:TaskbarIcon.ContextMenu>
               <ContextMenu>
                   <MenuItem Header="About Explicit Taskbar"
                             Command="{Binding OpenAboutCommand}">
                       <MenuItem.Icon>
                           <ui:SymbolIcon Symbol="Info20"/>
                       </MenuItem.Icon>
                   </MenuItem>
                   
                   <Separator />
                   
                   <MenuItem Header="Close"
                             Command="{Binding CloseCommand}">
                       <MenuItem.Icon>
                           <ui:SymbolIcon Symbol="ArrowExit20"/>
                       </MenuItem.Icon>
                   </MenuItem>
               </ContextMenu>
           </tb:TaskbarIcon.ContextMenu>
        </tb:TaskbarIcon>
</Window>
